[package]
name = "voxtrans-core"
version = "0.1.0"
edition = "2024"

[dependencies]
parakeet-rs = { version = "0.3.5", default-features = false, features = ["cohere", "directml", "ort-defaults"] }
hound = "3.5"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
