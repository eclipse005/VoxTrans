[workspace]
members = [
    "src-tauri",
    "voxtrans-core",
]
resolver = "2"
default-members = ["src-tauri"]

[patch.crates-io]
candle-kernels = { path = "vendor/candle-kernels" }
cudaforge = { path = "vendor/cudaforge" }
