[package]
name = "parakeet-rs-upstream-test"
version = "0.1.0"
edition = "2024"

[dependencies]
parakeet-rs = { version = "0.3", default-features = false, features = ["cuda", "load-dynamic"] }
hound = "3.5"
